name: Check Drone CI Signature

on:
  push:
    paths:
      - ".drone.yml"
  pull_request:
    paths:
      - ".drone.yml"

jobs:
  drone-signature-check:
    uses: grafana/shared-workflows/.github/workflows/check-drone-signature.yaml@b183aaddace903b558adf075d6d977085a4df848
